// Entidad de aplicación para futuras funcionalidades
export class AppInfo {
  constructor(name, version, description) {
    this.name = name;
    this.version = version;
    this.description = description;
  }
}

export default AppInfo;
